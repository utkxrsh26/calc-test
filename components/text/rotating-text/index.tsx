// Re-export the shadcn rotating-text implementation so other files can import from
// `@/components/text/rotating-text` as requested.

export { RotatingText, type RotatingTextProps } from '@/components/ui/shadcn-io/rotating-text';

// Intentionally no default export use named import: { RotatingText }
